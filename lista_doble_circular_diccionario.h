//
// Created by furfa on 14/9/2022.
//

#ifndef COMPILACION_CODIGOS_LISTA_DOBLE_CIRCULAR_DICCIONARIO_H
#define COMPILACION_CODIGOS_LISTA_DOBLE_CIRCULAR_DICCIONARIO_H
/*
typedef struct Nodo{
    int dato;
    struct Nodo * sig;
    struct Nodo * ant;
}nodo;
typedef struct Lista{
    nodo * primero;
    nodo * ultimo;
    int tamanio;
}lista;
lista * newlista();
nodo * newNodo(int numero);
void agregarNodo(lista * lista1,nodo * nodo1);
void mostrar(lista * lista1);
void eliminar(lista * lista1,int valorAeliminar);
void eliminarrepetido(lista * lista1);
void OrdenarLista(lista * lista1);
int miembro(lista * lista1,int valorAbuscar);
*/
 #endif //COMPILACION_CODIGOS_LISTA_DOBLE_CIRCULAR_DICCIONARIO_H
